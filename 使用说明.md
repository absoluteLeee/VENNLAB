# VENNLAB 使用说明

ABio 系列 · 集合交集与维恩图工具  
适用版本：2026-09-19 整理版

## 1. 软件用途

VENNLAB 比较 2、3 或 4 个集合，展示独有元素与共同元素。它进行集合运算，不执行富集显著性检验，也不自动识别物种或转换基因 ID。

## 2. 启动

完整解压后，用现代浏览器打开 `Venn.html`。整理版也可以双击 `start.cmd`。无需安装 Python、R 或 Node.js；脚本和主要界面字体放在软件目录中，可离线使用。

请保留 `fonts/`、`lib/` 和 `1.png`，不要只复制 HTML。图中文字使用系统 Arial 时，其他电脑的显示可能略有不同。

## 3. 界面布局

- **左侧：数据输入**。选择集合数量，填写集合名称和元素。
- **中间：可视化与结果**。查看主图、导出、保存记录及查看交集明细。
- **右侧：图形与颜色**。调整三集合布局、各集合颜色和透明度、背景、标题/数字字号及白边。

顶部标题栏固定显示；窄屏下各区域会自动调整位置。

## 4. 输入数据

1. 选择“2个集合”“3个集合”或“4个集合”。
2. 修改集合名称，例如 Control、Treatment。
3. 每行输入一个元素，或从 Excel 的单列复制粘贴。
4. 输入后自动更新，无需额外点击运行。

示例：

| 集合 A | 集合 B |
|---|---|
| TP53 | BRCA1 |
| BRCA1 | MYC |
| EGFR | EGFR |

结果为：仅 A 有 TP53，仅 B 有 MYC，A 与 B 共同包含 BRCA1 和 EGFR，总计 4 个不同元素。

注意：

- 同一集合内的重复元素只计一次，空行忽略，行首行尾空格会去除。
- 匹配区分大小写，`TP53` 与 `tp53` 会被视为两个元素。
- 每行是一个完整元素，不要把多个基因用逗号放在同一行，也不要粘贴表头。
- 软件不做 ID 映射；不同集合请使用一致的 ID 类型和命名格式。
- “清空”会清除输入并恢复相关默认设置，操作前请保存需要保留的数据。

## 5. 调整图形

- **布局**：三集合可切换页面提供的不同布局；四集合使用 jvenn 经典四椭圆布局。
- **颜色和透明度**：右侧分别调整各集合。默认透明度为 75%，已保存记录会恢复其原有设置。
- **背景**：可开关背景、选择颜色和调整背景透明度。
- **字号**：分别调整集合名称与区域数字的字号。
- **白边**：显示或隐藏形状轮廓；四集合白边绘制在填色上层。
- **区域数字颜色**：结果表中的色块可调整对应区域数字颜色。

维恩图使用固定布局，形状面积不按集合大小同比缩放；准确数量请看数字和结果表。

## 6. 读取交集结果

图中的数字表示对应独立区域的元素数量。点击数字，下方文本框显示元素，便于复制到 Excel。

四集合最多有 15 个独立区域，例如：

- “A（不含其他集合）”：只存在于 A。
- “A ∩ B（不含其他集合）”：在 A、B 中，但不在 C、D 中。
- “A ∩ B ∩ C ∩ D”：四个集合共同拥有。

四集合结果表的行也可以点击查看元素。如果要计算所有 A∩B 元素，需合并涉及 A、B 的各独立区域，包括同时属于 C 或 D 的部分；不能只读取 A、B 独立相交区域。

“总计”为各集合并集的不同元素数量，而不是各集合数量直接相加。

## 7. 导出图片

点击主图右上角“导出”，选择格式：

| 格式 | 用途与说明 |
|---|---|
| PNG | 通用位图，当前导出为 1000×1000 像素 |
| PDF | 打开浏览器打印窗口，在目标打印机中选择“另存为 PDF” |
| TIFF | 位图格式，当前为 1000×1000 像素 RGB，不保留透明通道 |
| SVG | 矢量图，可继续编辑；文件未内嵌字体，换电脑可能替换字体 |

TIFF 建议先开启白色背景并设为 100%，避免透明背景在不支持透明的格式中出现非预期底色。PDF 如未弹出窗口，请检查浏览器是否拦截弹窗；必要时在打印设置中关闭页眉页脚。

## 8. 保存和恢复

在主图下方输入保存名称，点击“保存”。记录列表会显示已保存项目，点击名称可恢复输入与设置；点击删除符号删除该条记录。

记录保存在**当前浏览器的 localStorage**，并非软件目录中的文件：

- 复制软件文件夹不会同时复制记录。
- 换浏览器、无痕模式、清理浏览器数据或更换打开路径，可能无法访问原记录。
- 建议另外保存原始输入文本和导出的图片；浏览器记录不要作为唯一备份。

## 9. 常见问题

- **某些元素没有进入交集**：检查大小写、空格、ID 类型以及是否把多个元素放在一行。
- **四集合无法显示**：确保 `lib/jquery.min.js`、`lib/canvas2svg.js`、`lib/jvenn.min.js` 完整；使用现代浏览器刷新。
- **找不到导出按钮**：按钮位于中间主图右上角，点击后展开格式菜单。
- **图片字体在另一台电脑变化**：SVG 未内嵌字体；优先使用 PNG/TIFF 保留外观，或在编辑软件中配置相同字体。
- **网页更新后仍显示旧样式**：尝试 Ctrl+F5 强制刷新。

本说明描述当前功能。项目许可证暂未决定，第三方库和字体仍遵循其各自许可。
